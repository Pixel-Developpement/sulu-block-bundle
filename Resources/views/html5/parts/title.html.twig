{% set block_property = (block_property is defined) ? block_property : 'title' %}
{% set element = (element is defined) ? element : 'h2' %}

{% if block[block_property] is defined and block[block_property] is not empty %}
    <{{ element }} property='{{ block_property }}'>{{ block[block_property] }}</{{ element }}>
{% endif %}
